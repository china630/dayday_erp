{
  "name": "dayday-erp",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -k -n api,web -c blue,magenta \"npm run dev:api\" \"wait-on tcp:127.0.0.1:4000 && npm run dev:web\"",
    "dev:api": "dotenv -e .env -o -- npm run start:dev -w @dayday/api",
    "dev:web": "dotenv -e .env -o -- npm run dev -w @dayday/web",
    "dev:web:clean": "dotenv -e .env -o -- npm run dev:clean -w @dayday/web",
    "restart:web": "npm run stop:next && dotenv -e .env -o -- npm run dev:clean -w @dayday/web",
    "restart:api": "npm run stop:api && dotenv -e .env -o -- npm run start:dev -w @dayday/api",
    "stop:next": "npx kill-port 3000",
    "stop:api": "npx kill-port 4000",
    "build": "npm run i18n:audit && npm run build -w @dayday/database && npm run build -w @dayday/api && npm run build -w @dayday/web",
    "db:generate": "npm run db:generate -w @dayday/database",
    "db:migrate": "npm run db:migrate -w @dayday/database",
    "db:migrate:dev": "npm run db:migrate:dev -w @dayday/database",
    "db:migrate:deploy": "npm run db:migrate:deploy -w @dayday/database",
    "db:migrate:reset": "npm run db:migrate:reset -w @dayday/database",
    "db:push": "npm run db:push -w @dayday/database",
    "db:execute": "npm exec -w @dayday/database prisma db execute --",
    "db:prisma": "npm exec -w @dayday/database prisma --",
    "db:seed": "npm run db:seed -w @dayday/database",
    "db:seed:demo": "cross-env SEED_DEMO_ORG=1 npm run db:seed -w @dayday/database",
    "db:dev-bootstrap": "npm run db:seed:demo && npm run db:sync-i18n && npm run db:prod-init -w @dayday/database",
    "db:setup": "npm run db:migrate:deploy && npm run db:dev-bootstrap",
    "db:ifrs-demo-map": "npm run db:ifrs-demo-map -w @dayday/database",
    "db:studio": "npm run db:studio -w @dayday/database",
    "db:sync-i18n": "npm run db:sync-i18n -w @dayday/database",
    "db:dump-to-prod": "npm run docker-init:export -w @dayday/database",
    "db:wipe-tenant": "npx tsx packages/database/prisma/wipe-tenant-data.ts",
    "db:seed:tivi": "npm run db:seed:tivi -w @dayday/database",
    "db:prod-init": "npm run db:migrate:deploy && npm run db:seed && npm run db:sync-i18n && npm run db:prod-init -w @dayday/database",
    "db:apply-template:ifrs": "npm run db:apply-template:ifrs -w @dayday/database",
    "docker-init:export": "npm run docker-init:export -w @dayday/database",
    "docker-init:super-admin-hash": "npm run docker-init:super-admin-hash -w @dayday/database",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "i18n:audit": "npx tsx scripts/i18n-audit.ts",
    "platform:billing-reconcile": "dotenv -e .env -o -- npx tsx scripts/billing-reconciliation.ts",
    "platform:dr-validate": "dotenv -e .env -o -- npx tsx scripts/dr-drill-validate.ts",
    "screenshots:erp": "dotenv -e .env -o -- npm run capture --prefix tools/playwright-screenshots --"
  },
  "dependencies": {
    "cross-env": "^10.1.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "dotenv-cli": "^8.0.0",
    "kill-port": "^2.0.1",
    "wait-on": "^8.0.1"
  },
  "engines": {
    "node": ">=22"
  }
}
